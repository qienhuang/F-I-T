# arXiv source (FIT scRNA commitment; Tier-2 / EST-gated)

This folder contains a minimal arXiv-compatible LaTeX source bundle:

- `main.tex`
- `abstract.tex`
- `content.tex`
- `references.bib`

To build a zip for upload:

```powershell
pwsh ./make_arxiv_zip.ps1 -Version "v0.1"
```

