# Analysis Notes

This directory is reserved for Path-4 fitting outputs:

- scale maps `f_{b->2b}` (isotonic + polynomial)
- fixed-point and slope bootstrap CI
- semigroup closure audit

Current commit focuses on data prep and gates. Model fitting can be added as `analysis/*.py` in the next step.

